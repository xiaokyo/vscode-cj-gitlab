一键合并workspace下的所有选择的项目到test,cn,com(环境也可选择), cn和com的情况要复制merge request的信息，默认全选

---

# 26年06月11日 14:21:17，第1次提交
👨‍💻**提交人：小K**

## 规划任务:

### 任务1：一键批量合并 workspace 多项目到多环境
- **入口**：webview 顶部新增「一键批量合并」按钮 → QuickPick 多选项目（默认全选，含 submodule）→ 多选环境 test/cn/com（默认全选）→ 执行。
- **合并行为**：test 与现有单项目一致（建 MR 后自动 accept）；cn/com 仅建 MR 不自动合并，并汇总复制 MR 信息到剪贴板。
- **失败处理**：单项目/单环境失败（未提交文件、已合并、目标分支不存在等）不中断整体，跳过并记录，全部跑完弹出汇总报告（成功/跳过/失败分组明细）。
- **复用资产**：`getAllWorkspaceProjectInfos`、`setTargetProjectByWorkspaceFolder`、`publishDevloperEnv`、`applyMergeRequest`、`findCnBranch/findProdBranch`、`checkStatusNoCommit`、`buildMergeInfo`（由 copyLink 文案下沉）。

🚀## 任务处理结果:

> ### 📄2606111 处理结果（完成内容）
> - **新增** `src/types/BatchPublish.ts`：`PublishEnv`/`BatchItemResult`/`BatchTarget` 类型定义。
> - **`src/GitlabService.ts`**：新增 `batchPublish(targets, envs, onProgress)` 编排循环（外层项目内层环境，每环境独立 try/catch，「已经合并」标记 skipped，未提交文件整项目 skip）；新增 `publishEnv` 单环境分发；新增 `buildMergeInfo(env, webUrl)` 文案拼装（test=测试/cn=线上(CN)/com=线上(COM)）。
> - **`src/CJGitlabView.ts`**：新增 `batchMerge()` 编排（双层 QuickPick canPickMany 默认全选 + withProgress 进度条 + 剪贴板汇总 + Modal 汇总报告）；`copyLink` 改调 `buildMergeInfo`（prod→com 兼容映射），消除重复文案逻辑；switch 注册 `batchMerge` 消息路由。
> - **`src/extension.ts` + `package.json`**：注册命令 `cj-gitlab.batchMerge`；版本号 0.1.29 → 0.1.30。
> - **`resources/webview/index.html` + `styles.css`**：`.quick-actions` 上方新增整宽「一键批量合并」按钮（复用 `#i-git-merge` 图标）。
> - **自测验证**：`yarn compile`（webpack）✅ compiled successfully 0 错误；`tsc --noEmit` ✅ No errors found。端到端手动验证待在扩展开发宿主（F5）中按多项目+submodule workspace 执行。
>
> 🛑**26年06月11日 14:21:17 第1次提交已完成**
